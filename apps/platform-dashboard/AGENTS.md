# Prototype Instructions

- Owner direction 2026-09-08: Templates Studio scrolls with the document in both library and saved-drafts tabs. Its outer shell must grow with content rather than clipping to 100dvh; preserve independent scrolling inside the phone preview and the fixed sidebar.

- Owner direction 2026-09-08: Template 6's phone camera inset uses its pale home background and saved background image, not the teal secondary brand color. Keep this consistent in Studio, draft editing and Store editing through the shared phone frame.

- Owner direction 2026-09-08: Keep admin sign-in on this browser/device for 30 days when the sign-in checkbox is enabled (enabled by default). Remembering email alone is insufficient. Use an HttpOnly cookie backed by revocable database state, never browser-stored passwords or Admin bearer tokens. Recover after backend restarts, revoke remembered access on logout, and keep transient connection failures on a retry screen instead of redirecting to login.

- Owner direction 2026-09-08: Saved drafts expose permanent deletion with a named confirmation. Physically delete the private draft and all draft-owned trial carts/orders, and update Studio counts and the resume strip after success. Only drafts whose store setup has not started are deletable; retain recovery state for started setups and actual Stores.

- Studio's live template preview and new creation editor must be identical before edits: use the backend's `creation_previews` values from the same `initialCreationValues` factory that creates drafts, the shared draft catalog mapper, 430 × 932 phone viewport, and selected locale. Expanded previews must use that same authenticated preset, never the separate DEV visual-demo route. Existing saved drafts remain authoritative and must not be reset to current presets.

- Owner reference update 2026-09-08: Templates Studio follows the supplied pale-blue library layout: breadcrumb/title, Store selector and Manage store, tabs above the search/category row, a three-column library beside a live phone preview, EN/AR and expand controls, and a latest-draft resume strip. Saved creation drafts remain in their own tab. Preserve actual template numbers, current imagery, Store-backed actions and real draft counts. This newer screenshot supersedes the earlier tabs-below-search placement.

- Owner direction 2026-09-08: use a realistic metal phone housing in the storefront editor, with camera glass, side buttons, rounded screen and natural shadows. Decorative hardware must not intercept preview interactions. Preserve the selected screen dimensions and Template 2's gap-free top edge.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## LabibTech owner-admin boundary

- Owner direction 2026-09-08: Template 5's Store design & typography panel exposes independent page/navbar colors and body/display font choices, with immediate valid hex editing and reset. Persist appearance in the Store's versioned template draft and forward it to the real renderer. Keep original defaults and preserve unsaved user edits during QA.

- Creation recovery 2026-09-08: show saved confirmation steps and use Resume store setup for interrupted drafts. Once owner/store provisioning is checkpointed, omit the temporary-password prompt. Keep saved identity fields locked and retain the exact original store, catalog and design. Template 5/6 development registry migrations are now applied; older pending-migration notes describe earlier slices only.

- This application is the platform-owner administration surface, not the merchant/vendor dashboard.
- Preserve the approved Arabic RTL, near-black navy, electric-blue cybersecurity design language.
- Keep Medusa implementation details and branding invisible in the interface.
- Platform authentication uses Medusa's `user` actor with a secure session; never place an Admin API token or server secret in browser code.
- Signing out of the Medusa-powered owner Admin must return to the LabibTech platform sign-in page; the built-in Medusa login screen must never be exposed.
- Keep the complete sign-in composition proportionally filled at normal desktop heights: no clipping, no page scroll, and no large auto-spacer between the submit button and trust footer.

## Selected owner control-plane direction

- Owner direction 2026-09-08: Add New Store separates store category from template: details/category → design/setup → owner/access → review. Include category-matched sample products by default for Professional Commerce, with an explicit empty-catalog option. Show the sample count before confirmation, use each template's default palette until customized, and create through the resumable database-draft workflow. Each store owns independent editable products/categories/variants/stock; publication stays separate. Preserve old drafts and existing catalogs.

- Owner direction 2026-09-08: Template 2's phone preview starts directly at the storefront header, without the blank camera inset or simulated island. Other template previews retain their existing camera spacing.

- Owner direction 2026-09-08: simplify Add New Store and Add Vendor to supported actions. Store setup uses details, owner access, all six numbered Studio templates with commerce settings, and review. Remove unsaved logo/business-type inputs and the misleading draft-activation toggle. Vendor setup separates account details, Store role assignment, and review. Keep template publication separate and preserve canonical provisioning and membership APIs.

- Owner direction 2026-09-08: omit the sidebar's Control center / Your platform, at a glance block; navigation follows the brand row directly.

- Owner direction 2026-09-08: Templates Studio leads with its title, controls and template gallery. Place resumable creation drafts below the Studio content, never above the page heading or gallery.

- Owner update 2026-09-07: add a shared labeled sidebar matching the soft-gray/cyan Admin dashboard, with Workspace, Management and Platform groups, active-route styling, collapse control and an account/sign-out footer. Phones use a keyboard-accessible navigation drawer. Keep the navbar removed and the dashboard full-page; dedicated Storefront editors/previews remain focused without dashboard navigation. All links use the existing guarded dashboard router.

- Owner direction 2026-09-07: temporarily remove the platform Admin navbar and sidebar on all dashboard routes, pending replacement navigation instructions. The dashboard surface fills the entire viewport with no outer inset, capped-width frame, rounded outer container, or outer shadow. Retain internal content spacing and existing page controls. This supersedes the previous shared navigation/frame geometry; sign-in and storefront phone editors keep their own layouts.

- Template 6 Explore follow-up 2026-09-06: optional bounded `brands.promotion_heading`, `promotion_subheading` and nullable `promotion_image_url` use explicit inline-edit targets and must survive confirmed-store normalization and the preview mapper. Category banners stay separate from home creator photos. New creation drafts now seed six reference categories with the same four products; never reseed old drafts. Database confirmation acceptance and the registry migration are still pending.

- Template 6 product-detail follow-up 2026-09-06: reuse the existing creation product gallery editor (previously URBX-only), keeping gallery photos independent from the original home thumbnail. New drafts have three Premium Hoodie photos; older drafts remain unchanged until explicitly edited. Do not claim a database migration or authenticated saved-store acceptance for this frontend slice.

- Template 6 follow-up 2026-09-06: home now uses the second Design2 reference while welcome remains separately reachable. The existing home-field dialog initializes compatible defaults for old drafts without overwriting welcome or another language. Four products/three categories are seeded only for new creation drafts, with prior empty drafts preserved. The original welcome Studio cover remains. Do not claim database acceptance or connected social/AI/location services.

- Owner addition 2026-09-06: append `template-6` as Template 6 without renumbering existing cards. Its first welcome screenshot is the Studio cover. Reuse the shared centered phone, explicit click-to-edit hero fields, saved name/accent and creation-draft workflow. It is welcome-only with no invented starter catalog. New registry constraint migration and DB confirmation acceptance remain pending; do not present this as a completed commerce template.

- Owner direction 2026-09-05: Templates Studio uses compact cards and numbered display names, Template 1 onward. Keep the visible-key order append-only for stable numbers (Drops, Luxe Full, Standard, Glow Beauty initially), retain categories, and use matching numbering in selection/search. This is presentation-only: do not rename saved Store identities or registry keys. Show up to four cards per desktop row instead of oversized two-column covers.

- Owner direction 2026-09-05: the four Templates Studio gallery covers and selected-template thumbnail use premium commercial device mockups generated from screenshots of the actual corresponding storefronts. Keep a consistent 3:2 family with distinct beauty/fashion/sneaker/luxury art direction. These are promotional assets only; do not redesign storefronts or replace functional preview/editor routes when updating gallery artwork. Keep originals recoverable and generation prompts alongside asset provenance.

- Latest owner amendment 2026-09-05: all four visible Studio choices (Glow Beauty, Standard, Drops, Luxe Commerce — Full Source) create immediate, resumable private database drafts through one shared centered-phone editor. The selected template keeps its own document and starter catalog. Click-to-edit, name/colors and owner/setup changes persist before confirmation; canonical owner/store/catalog provisioning is idempotent and publication stays separate. This supersedes earlier Glow-only creation scope, not the phone-fit or original-design requirements.

- Latest owner update 2026-09-05: add a compact, collapsible Store name & colors control beside Glow Beauty's centered phone. It edits canonical Store name, accent/button color and background tint, with ready-made palettes and immediate preview. This supersedes the earlier prohibition on applying selected brand colors to this canvas, not the original English layout/fonts or phone-fit requirements. Name/brand saves use the existing configuration API (live settings); content publication remains separate.
- Owner-approved update 2026-09-05: Glow Beauty creation first persists a private resumable creation draft, edited in the centered original-design phone. Name/colors, owner selection, content and starter catalog changes autosave with revisions. Explicit confirmation provisions exactly one owner-bound Store and independent real starter Products/categories/variants/stock using the existing images; it preserves all saved content and leaves publication separate. This supersedes the earlier no-starter-inventory creation instruction. Trial carts/orders are DB-persisted separately and must never become merchant sales, change stock, charge payments or send notifications. Other template creation paths remain unchanged.

- The owner dashboard is a SaaS control plane, never a dashboard for one Store.
- Owner direction updated 2026-08-10: `/dashboard` is an English LTR platform overview matching the supplied soft-gray LabibTech reference. Its primary navigation is `Dashboard`, `Stores`, `Vendors`, `Plans`, and `Activity`; the body prioritizes platform Store count, vendor count, Store growth, platform GMV/revenue connection states, subscriptions, and recent Stores.
- Owner direction updated 2026-08-10: the screenshot-matched English LTR soft-gray control-plane family now includes `/dashboard`, `/dashboard/clients` (Stores), `/dashboard/vendor-accounts` (Vendors), `/dashboard/storefronts` (Templates Studio), `/dashboard/settings`, and `/dashboard/security`. These pages must share invariant shell geometry so navigation never shifts the container.
- The reference's exact figures are allowed only when both the DEV-only `VITE_ENABLE_ADMIN_VISUAL_PREVIEW=true` build flag and `?demo=1` are present. A query parameter alone must never bypass authentication or enable fixtures. Authenticated real mode must use portfolio-backed Store/vendor facts and visibly mark GMV, revenue, uptime, or historical series unavailable until those platform feeds exist.
- The platform owner manages the hierarchy Client/Tenant -> Store -> storefront -> domain/hosting -> deployment -> vendor accounts -> subscription.
- The prior Arabic RTL tenant-portfolio screen remains historical implementation context; the shared English LTR control-plane shell and the supplied page references are the visual source of truth for the six modern routes above.
- Platform-level navigation prioritizes Store requests, clients and Stores, storefront design, domains/hosting, deployments, vendor access, plans/billing, operations, security, and platform settings.
- Store commerce operations remain available through a separate Store workspace; do not mix platform portfolio metrics with one Store's orders or sales.
- Never fabricate backend state. Features deferred beyond the current phase must be labeled as unavailable or not yet connected while preserving links to working Medusa functionality.
- Storefront Studio navigation is per Store and versioned with the Storefront document. Expose only the approved customer destinations, with Arabic/English labels, visibility, and ordering; never accept arbitrary URLs or expose platform-Admin settings through Store navigation.
- Storefront Studio brands are per Store and versioned with the Storefront document. Owners can edit the bilingual section copy, add/remove/reorder brand cards, and use either an uploaded safe image or a text-only brand name; deleting all cards must remove the entire customer-facing section without reserved space.
- Storefront Studio must avoid nested editor-shell scrolling. At desktop editor widths, the control-plane page owns the editor's vertical scroll so the complete form, actions, and selected device can always be reached from any pointer position; the field column expands with its content instead of trapping a second scroll. The customer Storefront iframe continues to own customer-page scrolling inside the device.
- In Storefront Studio, the inline mobile `Fit` preview prioritizes a readable, screenshot-matched phone width and keeps canvas scrolling disabled; the `Preview store` full-screen view must fit the complete selected device frame without clipping or panning. `100%` mode may expose an explicitly pannable preview canvas for pixel-level inspection.
- The mobile preview's Dynamic Island must occupy a reserved frame inset above the Storefront iframe, never overlap page titles or controls. Keep the selected device height and Fit scaling accurate; do not restore simulated time, battery, or signal indicators.
- Owner direction updated 2026-09-05 (supersedes the temporary read-only preview): Glow Beauty's local Studio is a phone-only visual editor using the original `/?preview=1&template=glow-beauty` renderer. Hide dashboard chrome and all permanent settings/language/identity panels; center the complete phone and fit BOTH viewport dimensions, with scrolling inside the storefront only. Click supported text/images in the phone to open a temporary content dialog; keep a compact Browse/Edit switch and Save draft action. Bind edits to the existing Store-scoped canonical draft, preserving other languages, branding, and published data. Do not apply the live profile's RTL, fonts, theme or catalog to this design canvas. Product/order fixtures remain explicitly preview-only; this canvas is development-only, not a production deployment.
- Owner direction updated 2026-09-04: the desktop Storefront Studio editor uses one unified white card with a compact title/action header, a left section rail, a focused center form, and a large right live-preview area. Keep the preview toolbar on one row, use a modern phone bezel with a Dynamic Island instead of browser traffic-light chrome, and avoid checkerboard preview backgrounds. Preserve the real Store-scoped draft/save/publish and live Storefront bridge while matching this geometry.
