# Storefront Instructions

## Reusable template fidelity

- Owner direction 2026-09-08: Template 4's shared navbar follows locale direction. Arabic begins with Home on the right and flows right-to-left on every page; preserve the saved navigation order and English layout.

- Owner direction 2026-09-08: Start the Arabic version of Template 4 with its hero: mirror the hero photograph horizontally, place heading/caption/body/button on the right in RTL, and point the action arrow left. Apply only to Arabic locale in the shared renderer; English retains its original composition.

- Owner direction 2026-09-08: Template 6 does not show the Try AI Search action or its assistant dialog. Apply this in the shared home renderer so Studio, editor and storefront agree; keep ordinary keyword search and product filters.

- Template 6 loading correction 2026-09-08: its saved home promotion eyebrow/heading/detail are intentionally empty bilingual strings. The public profile/preview parser must accept those empty fields while retaining type, length and plain-text validation for populated values. Do not replace them with promotional filler; rejecting them leaves the editor waiting for a valid profile.

- Owner direction 2026-09-08: Template 4 (`glow-beauty`) mounts one shared floating bottom navbar in App; the standalone DEV entry mounts it once around its page content. Never add page-local docks. Preserve published navigation labels/order/counts and route selection. Keep 12px side/bottom clearance plus the bottom safe area, all corners rounded, and purchase/cart/checkout actions above the dock.

- Owner direction 2026-09-08: Template 3's bottom navigation is mounted once in App, outside page content and route animations, on every route. Page components must not mount their own docks. Resolve saved labels/order/visibility through the shared navigation authority and derive active state from the route. Reserve dock clearance and position product/cart/checkout actions above it; keep the circular cart icon centered and white when active.

- Owner direction 2026-09-08: Template 3 (`standard`) uses larger two-column mobile product cards with portrait images and consistent readable copy, a horizontally scrolling category row, and roomier banners/section spacing. Desktop arrivals retain four larger cards with full-width category spacing. Apply this shared CSS to Studio, editor and storefront; do not restore the older compressed three/four-column mobile lanes.

- Owner direction 2026-09-08: Template 5 appearance settings independently control navbar background/text/active colors, main/heading/secondary/button text, panels, and body/display fonts across pages. Use allowlisted bundled/system font keys and optional versioned appearance values with original fallbacks; preserve text baked into images. Preview changes live and retain them through Store draft saving and publication.

- Owner direction 2026-09-08: Template 5 (URBX) bottom navigation stays fixed to the viewport while scrolling. Keep it aligned to the responsive storefront width, reserve content clearance, and include the mobile bottom safe area in the shared navigation styling.

- Template 6 order details amendment 2026-09-07: `/order-details` and `/order-details/:displayNumber` use the supplied receipt design and App's single fixed dock. Track Order from confirmation/profile and saved-order links navigate here. Public display numbers only select already-authorized receipts; never replace private tracking grants with number-based lookup. Keep merchant progress, purchased variants, actual amounts/payment state and existing guest PII reduction authoritative. Arafat/UAE/AED/ZARA/address/paid Visa samples remain standalone DEV-only. Downloads are explicitly order summaries, not tax invoices or payment proof; contact/invoice/cancellation requests require the store and never mutate orders automatically. No new backend or phase authority is granted.

- Template 6 favorites amendment 2026-09-07: `/favorites` uses the supplied masonry photo layout with shared FavoritesContext and complete current-catalog resolution. Search/category/price/sort/selection/removal are local list operations, not a new wishlist authority. Never auto-seed/refill customer favorites, invent live rating/location/currency data, or introduce a second dock. Standalone DEV highlights Profile on this screen; published live navigation remains authoritative.

- Template 6 profile amendment 2026-09-06: `/account` uses the supplied My Profile design and App's single shared dock. Favorites remain Store/browser-scoped; real order counts/progress use existing private order grants and reduced confirmation only. Arafat/three sample orders/address/Visa are standalone DEV-only. Preview profile edits are page-local, never saved customer identity. Accounts, saved cards/addresses, offers and reviews remain explicitly unconnected; do not fabricate their persistence or create a new backend authority.

- Template 6 checkout/navigation amendment 2026-09-06: `/checkout` implements the sixth Design2 reference with shared address/shipping/payment/completion/recovery authorities. The shared bottom dock is mounted once in App on every Template 6 route, with safe-area/content clearance; do not reintroduce page-local docks. Product Add to cart and Buy it now both use the selected quantity (1–20 minus the existing line quantity); only Buy it now navigates to checkout. This supersedes the older offer-contact action. Real/editor/trial checkout requires customer-entered details and capability-approved countries, shipping and COD/manual bank transfer. Arafat/UAE/AED/ZARA/sample Visa and reference shipping copy are standalone DEV-only, never tenant fallbacks. Sample Visa cannot submit any order: explicit COD selection is required. No card collection/charging or new payment service. Delivery instructions use the bounded canonical address field, not a new backend field. No DB, migration, live order or production acceptance is implied.

- Template 6 checkout/navigation amendment 2026-09-06: `/checkout` implements the sixth Design2 reference with shared address/shipping/payment/completion/recovery authorities. The shared bottom dock is mounted once in App on every Template 6 route, with safe-area/content clearance; do not reintroduce page-local docks. Product Add to cart and Buy it now both use the selected quantity (1–20 minus the existing line quantity); only Buy it now navigates to checkout. This supersedes the older offer-contact action. Real/editor/trial checkout requires customer-entered details and capability-approved countries, shipping and COD/manual bank transfer. Arafat/UAE/AED/ZARA/sample Visa and reference shipping copy are standalone DEV-only, never tenant fallbacks. Sample Visa cannot submit any order: explicit COD selection is required. No card collection/charging or new payment service. Delivery instructions use the bounded canonical address field, not a new backend field. No DB, migration, live order or production acceptance is implied.

- Template 6 category-results correction 2026-09-06: `/?category=<slug>` uses a focused category header, catalog-backed count, full-width single result or two-column multiple results, and a bottom-aligned dock with Categories active. Keep the original greeting/creator rail/masonry/AI layout only on the unfiltered home. Filters stay within the selected category; empty categories must remain truthful and link back to Explore. Category identity stays URL-backed and inline-editable; no catalog reseeding, imagery changes or backend authority is added.

- Template 6 Explore amendment 2026-09-06: `/categories` implements the fifth Design2 screenshot. Preserve the six-photo grid, tab/search/header anatomy, independent Fresh Finds banner and published dock. Complete catalog data drives search, brand lists and category links into the home results; only the current Store appears, never invented nearby sellers. New drafts get six categories and the existing four products; empty categories remain empty and old drafts are never reseeded. Optional strict `brands.promotion_*` fields and `category.<slug>.banner` editing must not change home/welcome imagery. No geolocation, new commerce authority, DB acceptance or migration activation is implied.

- Template 6 cart amendment 2026-09-06: `/cart` preserves the fourth Design2 screen with shared CartContext and FavoritesContext. Never seed/refill real carts, invent coupons, or select delivery from a design estimate. ZARA/Dubai/AED/Black fallback/alternate campaign crops and unselected AED20 estimate are standalone DEV-only. Real/editor/trial carts show saved product images/variants, Store identity, canonical totals and unknown shipping as At checkout. Both delivery Change and Proceed open shared checkout; neither submits an order. Keep photographic grid children width-constrained on narrow phones.

- Template 6 product-detail amendment 2026-09-06: all `/products/:handle` routes use the third Design2 image composition. Preserve the separate saved gallery, full-bleed cyan photo, seller card, size pills and bottom actions. Shared purchase options/cart remain authoritative; BUY IT NOW adds an available selected variant and opens checkout, never submits an order. Offers are contact-request drafts only. ZARA, Dubai, AED and review totals are standalone DEV-only; real stores show their own identity and currency. New drafts seed the premium gallery; never overwrite an older saved gallery or home thumbnail.

- Template 6 home amendment 2026-09-06 supersedes welcome-only routing: `/` and `/store` show the supplied discovery home; `/welcome` retains the first page. Preserve the generated photography, creator-style rail, staggered four-card grid, price pills and dock. Home content stays separate from welcome hero fields; use shared catalog/favorites/navigation and strict inline-edit keys. New drafts get four products/three categories; never reseed older empty drafts or borrow fixtures for live stores. Arafat, sample influencer identities, AED/location/rating/verification labels are standalone DEV-only. AI/location/story services remain unconnected with honest dialogs. Do not introduce accounts, geolocation, marketplace sellers or multi-currency commerce.

- Template 6 amendment 2026-09-06: the new `template-6` key renders the supplied turquoise welcome at `/`, `/welcome` and `/store`. Preserve generated scene/annotations, editable canonical hero text/image/CTA, proportional layout and native scroll. GET STARTED uses shared `/products`; later designs/catalog are pending, so never borrow another template's products. Reference pagination is decorative for now. UAE/GCC marketing copy is not permission for marketplace/geographic/currency expansion; shared Libya/LYD authority remains unchanged. Migration and DB confirmation acceptance are pending.

- URBX wishlist preview follow-up 2026-09-06: authenticated editor mode uses an isolated in-memory sample selection of up to four current draft products. Never seed customer favorites or persist sample selections to customer storage. Keep the selection shared across editor pages, honor removals without refilling, and resolve against current catalog edits/deletions. A genuinely empty draft catalog stays empty; no fixture fallback.

- URBX wishlist amendment 2026-09-06: `/favorites` uses the supplied two-column layout, native size selectors and lime Move to Cart controls. Shared FavoritesContext remains per-browser/per-Store and authoritative; never silently seed or refill real saved items. Refresh current product/purchase options before offering a variant. Remove a favorite only after successful shared cart addition, with a synchronous cross-card mutation lock. Preserve canonical product editor targets, Store currency and published navigation; standalone DEV sorting/dollars are not live overrides.

- URBX order-details amendment 2026-09-06: `/order-details` and `/order-details/:displayNumber` render the supplied page. Confirmation View Order now navigates here, superseding the older receipt-dialog note below. A public number is only a selector over already-authorized Store receipts, never a new lookup authority; merchant progress wins over the initial confirmation. Keep existing guest PII reduction: live address cards explain privacy, and sample address/delivery/dollars stay standalone DEV-only. Cancellation is an explicit contact request, never an automatic order mutation. No new courier, messaging or cancellation backend is authorized.

- URBX support amendment 2026-09-06: `/contact` preserves the supplied Help & Support design. Reuse the shared private order grants/trial reader, public Store contact configuration and existing product assets. Standalone DEV reference receipts are read-only, never live/editor/trial fallback. FAQs do not promise automatic cancellation, return eligibility or courier tracking. Contact actions open user-controlled drafts, not a simulated live-chat backend. Explicit `contact.heading` and `contact.body` editor keys preserve canonical bilingual fields; published navigation remains authoritative.

- URBX confirmation amendment 2026-09-06: `/order-confirmation` uses the generated package scene and the shared in-memory reduced receipt. Optional purchased `variant_title` travels with the canonical receipt; older receipts omit it, never infer it from the current catalog. The standalone DEV-only read-only design fixture must never submit an order, mutate the cart, or appear in live/editor/trial mode. Real email/delivery promises are not fabricated. View Order opens a receipt dialog and reads merchant progress only with the existing private tracking capability. Protected bank instructions remain post-confirmation only. No order submission, DB or live tracking acceptance was performed in this slice.

- URBX checkout amendment 2026-09-06: `/checkout` uses the supplied delivery/payment/summary design and shared CartContext address, shipping, completion and recovery authorities. Real stores and creation trials require entered addresses and current Store shipping/payment options; reference Alex/free delivery/arrival text is standalone DEV-only. Cards remain disabled, configured bank transfer remains supported, and bank instructions stay post-confirmation only. Customer addresses are not newly persisted in browser storage. Order submission browser acceptance requires explicit approval; the current slice checked only visual/address-edit behavior.

- URBX cart amendment 2026-09-06: `/cart` uses the supplied two-row black/lime design with the shared CartContext. Product links, selected variant labels, quantities and totals are Store-backed; do not auto-seed or refill real carts. Only standalone DEV previews use reference dollars/free shipping. Unselected real shipping stays "At checkout". Promo entry must not fabricate discounts: coupon authority remains outside this slice. Checkout still uses the shared screen pending its individual design.

- URBX categories amendment 2026-09-06: `/categories` uses two wide photographic panels plus two small cards from the supplied reference. Keep `brands.items[].banner_image_url` separate from home `image_url`; optional category copy and explicit `category.<slug>.banner` targets use the shared editor. Category counts come from the complete Store catalog, with slug-compatible membership after name edits. Do not fabricate the reference's 12/8/6/10 counts or seed extra products. Other unimplemented page designs and the pending registry migration remain separate.

- URBX categories amendment 2026-09-06: `/categories` uses two wide photographic panels plus two small cards from the supplied reference. Keep `brands.items[].banner_image_url` separate from home `image_url`; optional category copy and explicit `category.<slug>.banner` targets use the shared editor. Category counts come from the complete Store catalog, with slug-compatible membership after name edits. Do not fabricate the reference's 12/8/6/10 counts or seed extra products. Other unimplemented page designs and the pending registry migration remain separate.

- URBX shop amendment 2026-09-06: `/products` uses the supplied two-column Shop the Drop reference. Independent optional `content.shop` fields use the strict editor bridge; reuse the generated product imagery and Store-scoped catalog/purchase/cart APIs. Categories and prices must filter the complete fetched catalog, not only its first page. Store currency/navigation remain authoritative; reference dollars and Discover navigation are standalone DEV-only. Other destination designs remain pending.

- Owner amendment 2026-09-06: URBX is Template 5 / registry key 8. Implement supplied pages one at a time. Welcome is `/welcome`; home is `/` or `/store`. Home `content.home` is independent of welcome `content.hero`, with strict explicit inline-edit keys. Use generated artwork and shared store-owned catalog/purchase-options/CartContext. This home has four category columns and three best-seller columns. Other URBX routes currently use shared surfaces. No production, card-payment, accounts or broader phase approval is implied.

- Latest owner amendment 2026-09-05: the persisted creation/editor flow now applies to all four visible templates (Glow Beauty, Standard, Drops and Luxe Commerce — Full Source), superseding Glow-only scope below. Reuse each original renderer and images with the shared Store-scoped data/commerce and strict editor bridge. No live fixture fallback or fabricated review/payment/delivery claims. Keep template geometry and shared navigation; pages without an original implementation use shared commerce surfaces pending a separate design. Standalone DEV mock previews stay independent.

- Latest owner amendment 2026-09-05: the persisted creation/editor flow now applies to all four visible templates (Glow Beauty, Standard, Drops and Luxe Commerce — Full Source), superseding Glow-only scope below. Reuse each original renderer and images with the shared Store-scoped data/commerce and strict editor bridge. No live fixture fallback or fabricated review/payment/delivery claims. Keep template geometry and shared navigation; pages without an original implementation use shared commerce surfaces pending a separate design. Standalone DEV mock previews stay independent.

- Latest owner update 2026-09-05: Glow Beauty's design canvas also accepts the owner's selected Store name and accent/background colors through its validated parent bridge. This supersedes the earlier content-only restriction for those fields. Apply them across canvas pages without changing original geometry, fonts, English copy, or the standalone mock preview. Canonical name/brand configuration saves remain separate from draft publication; mock commerce stays preview-only.

- A storefront template is the complete customer experience, not a palette or image pack. Preserve the selected source design's shell geometry, layout, spacing, typography, motion, product-card anatomy, product details, favorites, cart, checkout, order confirmation, responsive behavior, and mobile navigation.
- Template previews in the owner dashboard must launch the actual `apps/storefront` renderer with mock Store/catalog/commerce data. Never maintain a simplified lookalike preview that can drift from the published implementation.
- Mock catalog, cart, checkout, and order data is permitted only in the DEV-only template preview. The owner-approved 2026-09-05 private creation-draft canvas instead uses saved catalog choices and isolated DB trial orders through a strict parent-origin/channel RPC, without exposing admin credentials. Confirmation installs independent real store records. Live storefront routes must continue to use Store-scoped backend data and must never fall back to fixtures.
- The original Glow Beauty components serve the creation canvas, confirmed-store editor and customer storefront. Keep the original English layout/Manrope typography and shared navigation, with only explicit owner name/color/content changes. Guest order status uses an opaque store-bound grant, not public order-number lookup; only merchant-saved manual steps can advance tracking.
- Owner direction 2026-09-05: Glow Beauty's visual editing canvas must use the original English design renderer with content-only edits, not a second layout or the live profile's locale/theme. In the admin phone, editable hero text/images, category labels/images and shared navigation labels are selected directly and saved to canonical Store draft fields. Validate iframe messages against the exact parent origin/source, channel and allowlisted fields. Preserve the canvas channel across internal preview pages only. Keep mock product/order data clearly separate from saved template content.
- Keep shared commerce APIs, Store isolation, and real production state outside template-specific presentational components. Templates may change rendering and interaction anatomy without forking the commerce authority.
- The Luxe Commerce reference source is `D:\Projects\السنوسي وأبنائه\apps\web`. Use it as the visual source of truth for the Luxe template while adapting data access to LabibTech's shared storefront API.
- Every template header and responsive navigation must render the Store's published, allowlisted navigation order and localized labels. `settings` always means customer account settings, never the platform Admin settings surface.
